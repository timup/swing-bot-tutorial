# Environment: development, testing, production
ENVIRONMENT=development

# Database configuration
POSTGRES_USER=swing_bot_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=swing_bot_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Logging
LOG_LEVEL=INFO
# LOG_FILE=logs/app.log  # Uncomment to enable file logging

# API settings (for FastAPI when implemented)
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=1

# Data paths
# DATA_DIR=/path/to/custom/data/directory  # Uncomment to override default
